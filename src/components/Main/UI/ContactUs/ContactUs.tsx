import { Container, Title } from '../../../../UI';

const ContactUs = () => {
  return (
    <section>
      <Container>
        <Title text={'Contact Us'} />
      </Container>
    </section>
  );
};

export default ContactUs;
